	<div class="well">
<div class="row">
	<div class="span3">
		<img src="http://placehold.it/200x200">
	</div>
	<div class="span6">
	  <ul class="nav">
		<li><b><%= @user.full_name %></b><br /></li>
		<li><b>Email:</b> <%= @user.email %></li>
	  </ul>
	</div>
	<div class="span2">
		<% if user_signed_in? %>
        <%= link_to "New Status", new_status_path, class: "btn" %> <a class="btn btn-small" href="<%= edit_user_registration_path %>"><i class="icon-cog"></i></a>
        <% else %>
        <% end %>
  </div>
</div>

</div>
<div class="well">
<% if @statuses %>
<% @statuses.each do |status| %>
<div class="status">
<div class="strong">
  <strong><%= status.user.full_name %> </strong><%= link_to time_ago_in_words(status.created_at) + " ago", status %>
  <p><%= status.content %></p>
    <span class="admin">
      <%= link_to "Edit", edit_status_path(status) %> |
      <%= link_to "Delete", status, method: :delete, data: {confirm: "Are you sure you want to delete this message?"} %>
  </div>
</div>
   <% end %>
  <% end %>
</div>
